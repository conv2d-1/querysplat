pipeline=dict(
    type='hAlgorithm.datasets_4d.pointodyssey_track_dataset.PointOdysseyTrackDataset',
    phase='vis',
    name='pointodyssey',
    seed=0,
    data_root='/mnt/cfsdata/Team/AI/datasets/TMD/',
    data_path='/mnt/cfsdata/Team/AI/datasets/TMD/PointOdyssey/test_mf_with_tracking.json',
    sampling_strategy=[0],
    train_transforms=[
        dict(type='hAlgorithm.datasets.transforms.transforms.Resize',
            width=630,
            height=476),
        dict(type='hAlgorithm.datasets.transforms.transforms.RandomHorizontalFlip',
            prob=0.5),
        dict(type='hAlgorithm.datasets.transforms.metric3d_transforms.PhotoMetricDistortion',
            to_gray_prob=0.1,
            distortion_prob=0.05),
        dict(type='hAlgorithm.datasets.transforms.metric3d_transforms.RandomBlur',
            prob=0.05),
        dict(type='hAlgorithm.datasets.transforms.metric3d_transforms.RGBCompresion',
            prob=0.1,
            compression=[0, 50]),
        dict(type='hAlgorithm.datasets.transforms.transforms.ToTensor'),
        dict(type='hAlgorithm.datasets.transforms.transforms.Normalize',
            mean=[127.5, 127.5, 127.5],
            std=[127.5, 127.5, 127.5])],
    test_transforms=[
        dict(type='hAlgorithm.datasets.transforms.transforms.ResizeKeepRatio',
            max_size=504,
            patch_size=14,
            is_lidar=False,
            low_resolution=True,
            backup=True),
        dict(type='hAlgorithm.datasets.transforms.transforms.ToTensor'),
        dict(type='hAlgorithm.datasets.transforms.transforms.Normalize',
            mean=[127.5, 127.5, 127.5],
            std=[127.5, 127.5, 127.5])],
    depth_scale=1.0,
    min_depth=0.001,
    max_depth=20.0,
    normalize_depth=dict(
        type='hAlgorithm.datasets.transforms.depth_transforms.ScaleShiftDepthNormalizer',
        norm_min=-1.0,
        norm_max=1.0,
        min_max_quantile=0.02,
        clip=True
    ),
    with_edge_mask=False,
    with_pointmap=True,
    sparse_depth_ratio=0,
    sky_index=-1,
    recalculate_normal=False,
    normal_transform=dict(
        type='hAlgorithm.datasets.transforms.norm_transforms.DepthToNormal'
    ),
    debug=False,
    size_pool=None,
    point_normalize_mode='distance',
    depth_invalid_sem_names=None,
    only_pointmap=True,
    mf_scene_sampling_strategy='end:10',
    mf_to_mv=True,
    clip_maxlen=None,
    clip_step=None,
    clip_sampler=dict(
        type='hAlgorithm.datasets_mv.clip_sampler.debug_trajectory.DebugTrajectory',
        view_num=4,
        seed=0
    ),
    sem_name='',
    with_pointmap_raw=True,
    interpolate_version=None,
    interpolate_k=0,
    normalize_cameras=True,
    with_global_scale=True,
    sparse_pattern=None,
    sparse_pattern_v2=None,
    sparse_pattern_v3=None
)

pipeline=dict(
    type='hAlgorithm.datasets_4d.kubric4d_track_dataset.Kubric4DTrackDataset',
    phase='vis',
    name='kubric4d',
    seed=0,
    data_root='/mnt/cfsdata/Team/AI/datasets/TMD/',
    data_path='/mnt/cfsdata/Team/AI/datasets/TMD/Kubric-4D/test_mf_with_tracking_2048.json',
    sampling_strategy=[0],
    train_transforms=[
        dict(type='hAlgorithm.datasets.transforms.transforms.Resize',
            width=630,
            height=476),
        dict(type='hAlgorithm.datasets.transforms.transforms.RandomHorizontalFlip',
            prob=0.5),
        dict(type='hAlgorithm.datasets.transforms.metric3d_transforms.PhotoMetricDistortion',
            to_gray_prob=0.1,
            distortion_prob=0.05),
        dict(type='hAlgorithm.datasets.transforms.metric3d_transforms.RandomBlur',
            prob=0.05),
        dict(type='hAlgorithm.datasets.transforms.metric3d_transforms.RGBCompresion',
            prob=0.1,
            compression=[0, 50]),
        dict(type='hAlgorithm.datasets.transforms.transforms.ToTensor'),
        dict(type='hAlgorithm.datasets.transforms.transforms.Normalize',
            mean=[127.5, 127.5, 127.5],
            std=[127.5, 127.5, 127.5])],
    test_transforms=[
        dict(type='hAlgorithm.datasets.transforms.transforms.ResizeKeepRatio',
            max_size=504,
            patch_size=14,
            is_lidar=False,
            low_resolution=True,
            backup=True),
        dict(type='hAlgorithm.datasets.transforms.transforms.ToTensor'),
        dict(type='hAlgorithm.datasets.transforms.transforms.Normalize',
            mean=[127.5, 127.5, 127.5],
            std=[127.5, 127.5, 127.5])],
    depth_scale=1.0,
    min_depth=0.001,
    max_depth=20.0,
    normalize_depth=dict(
        type='hAlgorithm.datasets.transforms.depth_transforms.ScaleShiftDepthNormalizer',
        norm_min=-1.0,
        norm_max=1.0,
        min_max_quantile=0.02,
        clip=True
    ),
    with_edge_mask=False,
    with_pointmap=True,
    sparse_depth_ratio=0,
    sky_index=-1,
    recalculate_normal=False,
    normal_transform=dict(
        type='hAlgorithm.datasets.transforms.norm_transforms.DepthToNormal'
    ),
    debug=False,
    size_pool=None,
    point_normalize_mode='distance',
    depth_invalid_sem_names=None,
    only_pointmap=True,
    mf_scene_sampling_strategy='end:10',
    mf_to_mv=True,
    clip_maxlen=None,
    clip_step=None,
    clip_sampler=dict(
        type='hAlgorithm.datasets_mv.clip_sampler.debug_trajectory.DebugTrajectory',
        view_num=4,
        seed=0
    ),
    sem_name='',
    with_pointmap_raw=True,
    interpolate_version=None,
    interpolate_k=0,
    normalize_cameras=True,
    with_global_scale=True,
    sparse_pattern=None,
    sparse_pattern_v2=None,
    sparse_pattern_v3=None
)

pipeline=dict(
    type='hAlgorithm.datasets_4d.hasim_track_dataset.HaSimTrackDataset',
    phase='vis',
    name='hasim',
    seed=0,
    data_root='/mnt/cfsdata/Team/AI/datasets/TMD',
    data_path='/mnt/cfsdata/Team/AI/datasets/TMD/Hasim_pinhole_4D/hasim_4d_mf_val_all_frames_with_tracking.json',
    sampling_strategy=[0],
    train_transforms=[
        dict(type='hAlgorithm.datasets.transforms.transforms.Resize',
            width=630,
            height=476),
        dict(type='hAlgorithm.datasets.transforms.transforms.RandomHorizontalFlip',
            prob=0.5),
        dict(type='hAlgorithm.datasets.transforms.metric3d_transforms.PhotoMetricDistortion',
            to_gray_prob=0.1,
            distortion_prob=0.05),
        dict(type='hAlgorithm.datasets.transforms.metric3d_transforms.RandomBlur',
            prob=0.05),
        dict(type='hAlgorithm.datasets.transforms.metric3d_transforms.RGBCompresion',
            prob=0.1,
            compression=[0, 50]),
        dict(type='hAlgorithm.datasets.transforms.transforms.ToTensor'),
        dict(type='hAlgorithm.datasets.transforms.transforms.Normalize',
            mean=[127.5, 127.5, 127.5],
            std=[127.5, 127.5, 127.5])],
    test_transforms=[
        dict(type='hAlgorithm.datasets.transforms.transforms.ResizeKeepRatio',
            max_size=504,
            patch_size=14,
            is_lidar=False,
            low_resolution=True,
            backup=True),
        dict(type='hAlgorithm.datasets.transforms.transforms.ToTensor'),
        dict(type='hAlgorithm.datasets.transforms.transforms.Normalize',
            mean=[127.5, 127.5, 127.5],
            std=[127.5, 127.5, 127.5])],
    depth_scale=1.0,
    min_depth=0.001,
    max_depth=20.0,
    normalize_depth=dict(
        type='hAlgorithm.datasets.transforms.depth_transforms.ScaleShiftDepthNormalizer',
        norm_min=-1.0,
        norm_max=1.0,
        min_max_quantile=0.02,
        clip=True
    ),
    with_edge_mask=False,
    with_pointmap=True,
    sparse_depth_ratio=0,
    sky_index=-1,
    recalculate_normal=False,
    normal_transform=dict(
        type='hAlgorithm.datasets.transforms.norm_transforms.DepthToNormal'
    ),
    debug=False,
    size_pool=None,
    point_normalize_mode='distance',
    depth_invalid_sem_names=None,
    only_pointmap=True,
    mf_scene_sampling_strategy='all',
    mf_to_mv=True,
    clip_maxlen=None,
    clip_step=None,
    clip_sampler=dict(
        type='hAlgorithm.datasets_mv.clip_sampler.debug_trajectory.DebugTrajectory',
        view_num=4,
        seed=0
    ),
    sem_name='',
    with_pointmap_raw=True,
    interpolate_version=None,
    interpolate_k=0,
    normalize_cameras=True,
    with_global_scale=True,
    sparse_pattern=None,
    sparse_pattern_v2=None,
    sparse_pattern_v3=None
)

pipeline=dict(
    type='hAlgorithm.datasets_4d.hasim_track_dataset.HaSimTrackDataset',
    phase='vis',
    name='hasim_benchmark_running',
    seed=0,
    data_root='/mnt/cfsdata/Team/AI/datasets/TMD',
    data_path='/mnt/cfsdata/Team/AI/datasets/TMD/Hasim_benchmark/Running/mf_tracking.json',
    sampling_strategy=[0],
    train_transforms=[
        dict(type='hAlgorithm.datasets.transforms.transforms.Resize',
            width=630,
            height=476),
        dict(type='hAlgorithm.datasets.transforms.transforms.RandomHorizontalFlip',
            prob=0.5),
        dict(type='hAlgorithm.datasets.transforms.metric3d_transforms.PhotoMetricDistortion',
            to_gray_prob=0.1,
            distortion_prob=0.05),
        dict(type='hAlgorithm.datasets.transforms.metric3d_transforms.RandomBlur',
            prob=0.05),
        dict(type='hAlgorithm.datasets.transforms.metric3d_transforms.RGBCompresion',
            prob=0.1,
            compression=[0, 50]),
        dict(type='hAlgorithm.datasets.transforms.transforms.ToTensor'),
        dict(type='hAlgorithm.datasets.transforms.transforms.Normalize',
            mean=[127.5, 127.5, 127.5],
            std=[127.5, 127.5, 127.5])],
    test_transforms=[
        dict(type='hAlgorithm.datasets.transforms.transforms.ResizeKeepRatio',
            max_size=504,
            patch_size=14,
            is_lidar=False,
            low_resolution=True,
            backup=True),
        dict(type='hAlgorithm.datasets.transforms.transforms.ToTensor'),
        dict(type='hAlgorithm.datasets.transforms.transforms.Normalize',
            mean=[127.5, 127.5, 127.5],
            std=[127.5, 127.5, 127.5])],
    depth_scale=1.0,
    min_depth=0.001,
    max_depth=20.0,
    normalize_depth=dict(
        type='hAlgorithm.datasets.transforms.depth_transforms.ScaleShiftDepthNormalizer',
        norm_min=-1.0,
        norm_max=1.0,
        min_max_quantile=0.02,
        clip=True
    ),
    with_edge_mask=False,
    with_pointmap=True,
    sparse_depth_ratio=0,
    sky_index=-1,
    recalculate_normal=False,
    normal_transform=dict(
        type='hAlgorithm.datasets.transforms.norm_transforms.DepthToNormal'
    ),
    debug=False,
    size_pool=None,
    point_normalize_mode='distance',
    depth_invalid_sem_names=None,
    only_pointmap=True,
    mf_scene_sampling_strategy='first:5',
    skip_static_threshold=0.02,
    static_traj_ratio=1.0,
    mf_to_mv=True,
    clip_maxlen=None,
    clip_step=None,
    clip_sampler=dict(
        type='hAlgorithm.datasets_mv.clip_sampler.debug_trajectory.DebugTrajectory',
        view_num=4,
        seed=0
    ),
    sem_name='',
    with_pointmap_raw=True,
    interpolate_version=None,
    interpolate_k=0,
    normalize_cameras=True,
    with_global_scale=True,
    sparse_pattern=None,
    sparse_pattern_v2=None,
    sparse_pattern_v3=None
)

